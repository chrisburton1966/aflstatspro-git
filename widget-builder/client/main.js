import React from 'react';
import ReactDOM from 'react-dom';
import WidgetBuilder from './scripts/scratch/containers/WidgetBuilder';

ReactDOM.render(
    <WidgetBuilder />,
    document.getElementById('main')
);